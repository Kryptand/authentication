import { Component, h} from '@stencil/core';

@Component({
  tag: 'kryptand-external-login-button',
  styleUrl: 'external-login-button.css',
  shadow: true,
})
export class ExternalLoginButton {
  render() {
    return <host/>;
  }
}
